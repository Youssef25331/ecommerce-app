import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF22A77D);
  static const Color primaryVariant = Color(0xFF1A8E6A);
  static const Color secondary = Color(0xFF939393);
  static const Color secondaryVariant = Color(0xFF1A8E6A);

  // Background and surface colors
  static const Color background = Color(0xFFFFFFFF);

  static const Color surface = Color(0xFFfafafc);
  static const Color edges = Color(0xFFDEDEDE);
  static const Color thickEdges = Color(0xFFCDCED2);

  // Text colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF393F42);

  // Accent colors
  static const Color accent = Color(0xFFD9D9D9);
  static const Color error = Color(0xFFA4001E);
}
